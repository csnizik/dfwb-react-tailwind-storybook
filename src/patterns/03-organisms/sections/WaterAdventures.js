import React from 'react';
import classNames from 'classnames';

const WaterAdventures = ({ pathName, ...props }) => {
  return (
    <section>
      <h1>WaterAdventures</h1>
    </section>
  );
};

export default WaterAdventures;
