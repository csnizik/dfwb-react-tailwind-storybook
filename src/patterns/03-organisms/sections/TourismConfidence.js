import classNames from 'classnames';
import React from 'react';

const TourismConfidence = () => {
  return (
    <main className={classNames('flex-grow')}>
      <h1>Tourism Confidence</h1>
    </main>
  );
};

export default TourismConfidence;
